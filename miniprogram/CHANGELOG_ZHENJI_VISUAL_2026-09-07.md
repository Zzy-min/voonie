# CHANGELOG — 真机/模拟器截图对照后的视觉修复（视觉对齐）

日期：2026-09-07

## 依据
用户 DevTools 截图（首页/日历/录音/广场/我的）+ 案例图对照；本机已同步云电脑最新包（萌宠/书架已补齐）。

## 本机现状核对
- **Tab（P1）已正确**：中心为「萌宠」爪印大键（index 2），选中色 `#D9845B`，底部含 `env(safe-area-inset-bottom)`；5 个 Tab 顺序 = 首页/日记/萌宠/书架/我的。**无需改动**。
- **录音页（P1）已正确**：`pages/record` 主体为倾听小狗 + 音波可视化 + 计时器 + 状态提示 + 底部「暂停 / 结束了 / 重新录制」；**无「和萌宠聊天」大卡**。**无需改动**。
- **旧文案（P2）已清理**：项目中已无「切换微色」「时光回忆」残留（早前已改为「切换版式」「时光胶囊」）；仅历史的 changelog 提及，非当前代码。「时光胶囊」（Profile，需求保留）与日历页标题「回忆时光」为保留项，未动。

## 本轮改动
### 首页 P0 — 移除中部大胶囊软 CTA
- `pages/index/index.wxml`：删除中部 `.cta-wrapper > .soft-cta`（「也可以按这里语音开启倾诉」大胶囊），改为注释说明——**底部输入条 + 麦克风为主交互**，避免截图那种大胶囊抢中间。
- `pages/index/index.wxss`：删除 `.cta-wrapper` / `.soft-cta*` 样式，并将 `.home-shortcuts` 的 `margin-top` 28→48，保持中部下延的纵向节奏。
- 保留：底部输入条（placeholder「说说今天发生的事…」）+ 麦克风按钮（`onMicTap` → `onStartRecord`）、萌宠/书架快捷 chip、草稿提示、问候气泡、小狗主角。
- 未删业务逻辑：`onStartRecord` 仍由 `onMicTap` / `onResumeDraft` 调用，功能完整。

## 打包体积（维持瘦身）
- `packOptions.ignore` 保留上一轮瘦身配置（reference、未引用图、`*.md`）。
- 当前打包候选 ≈ **475 KB**（< 1500KB 余量，远低于 2MB 上限）。

## 未改动 / 红线
- 未改 Tab 结构、登录/日记/日历/书架/我的等业务逻辑。
- 未写密钥；**未 git push**。

## 交付后操作
关掉开发者工具报错框后重新编译 / 真机预览；若本机目录与工作区不一致，以工作区为准或等需求方同步。